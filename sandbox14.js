// logical NOT (!) - reverses the boolean

let user = false;

if (!user) {
  // normal state is true, use logical NOT to switch to false, therefore matches value and logs to console
  console.log("you must be logged in to continue");
}

console.log(!true);
console.log(!false);
